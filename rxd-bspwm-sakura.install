post_install() {
	echo '**************************************************************************'
	echo         [*] DOT Files installed in /usr/share/rxd-sakura 
	echo [*] Make sure to run command " $ cp -r /usr/share/rxd-sakura/* ~/.config/ "
	echo [*]          before rebooting/relogin to see changes!
	echo '**************************************************************************'
}
